# 项目协作与 GitHub 同步规则

- 每次完成一个边界明确、可验证的功能优化或修复后，必须及时完成 Git 本地与远程同步。
- 提交前先完成与改动相称的检查，并确认工作区状态。
- 只暂存和提交本次任务直接涉及的文件；工作区中已有的无关改动、临时文件和用户文件不得混入提交。
- 使用清晰的提交信息说明功能或修复内容，并推送到当前项目已配置的远程仓库与当前分支。
- 推送成功后，向用户说明提交与远程同步已完成；推送受阻、远程未配置或改动边界无法安全区分时，必须如实说明原因并请求处理，不得声称已推送。
- 涉及密钥、Token、账号配置或本地数据时，提交前检查并避免将敏感信息或不应公开的数据提交到远程仓库。
